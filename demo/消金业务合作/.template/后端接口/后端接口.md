doctype:后端接口

# {{接口名}}（调用下游：{{下游系统}}）

> 本模板由「消金业务合作平台」定义。每个 .md 文件描述**一个外部接口**（本系统调用下游系统）。
> 调用下游的接口与本系统接口一样需**高风险评估**：是否关键接口、字段级高风险（金额/幂等/业务状态等）、幂等/防重、分页必须搭配排序、交易状态判断。

## 基本信息

| 项 | 值 | 备注 |
| --- | --- | :--- |
| 下游系统 | {{资金方-XX银行}} | 被调用的外部系统全称 |
| 方法 | {{GET/POST/PUT/DELETE}} | |
| 路径 | {{/api/loan/apply}} | 外部系统提供的路径 |
| 版本号 | {{v1.0.0}} | 格式 `vX.Y.Z` |
| 是否关键接口 | {{是/否}} | 满足任意 1 条即为「是」：①涉及资金交易 ②涉及短信发送 ③涉及监管报送或内部结算 ④包含高风险字段 |
| 该接口涉及的业务范围 | {{业务范围}} | 穷举所有涉及产品；不接受「标准化的所有产品」这类含糊描述 |
| 并发限制要求 | {{无要求 / TPS ≤ X}} | 下游系统对调用方的并发限制（默认为无要求） |
| 超时时间 | {{3s}} | 连接 / 读取超时 |
| 重试机制 | {{最多 3 次，指数退避}} | 仅幂等前提下允许重试 |
| 幂等规则 | {{[[数据库/loan/loan_apply#apply_no]]}} | 说明使用的幂等规则，引用下方高风险字段中的幂等字段 |
| 防止重复调用外部接口的机制 | {{[[数据库/...]]}} | 发送外部接口前更新交易记录状态（记录是否发送外部），并通过乐观锁避免重复调用 |
| 交易状态判断 | {{[[数据库/loan/loan_apply#status]]}} | 准则：明确失败才是失败，明确成功才是成功，其他应作为处理中 |

## 接口详情

字段说明：

| 字段 | 类型 | 长度 | 是否高风险字段 | 高风险字段类型 | 数据来源 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| {{field}} | {{bigint}} | {{20}} | {{是/否}} | {{金额/本金/利息/罚息/总额/单位/精度/四舍五入/还款方式/期次/业务状态/响应结果判断/发送前状态更新/幂等/防重/征信}} | {{[[数据库/...]] 或 [[接口文档/...]]}} | {{字段用途}} |

> 高风险字段（「是否高风险字段=是」）必须填写「数据来源」：来自 XX 表的 XX 字段、或本系统 XX 接口的 XX 字段响应，使用 `[[wikilink]]` 引用。

请求示例：

```json
{
  "{{field}}": 0
}
```

响应示例：

```json
{
  "{{code}}": 0,
  "{{msg}}": "success"
}
```

## 变更记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| v1.0.0 | {{2026-08-06}} | {{初版}} |
