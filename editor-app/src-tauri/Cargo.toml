[package]
name = "milkdown-note"
version = "0.1.0"
description = "Milkdown Note - Markdown 编辑器（Tauri + Vue + Crepe）"
edition = "2021"

[lib]
name = "milkdown_note_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
